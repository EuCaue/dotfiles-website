export type Data = {
  wm: {
    name: string
    config: string
    link: string
    version: string
    splash: string
  }
  bar: {
    name: string
    config: string
    link: string
    version: string
  }
  cursor: {
    name: string
    size: string
  }
  font: {
    name: string
  }
  term: {
    name: string
    config: string
    link: string
    version: string
  }
  multiplexer: {
    name: string
    config: string
    version: string
    link: string
  }
  editor: {
    name: string
    config: string
    link: string
    version: string
  }
  wallpaper: {
    link: string
  }
}
